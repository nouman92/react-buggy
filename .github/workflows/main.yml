name: CI
on:
  push:
  pull_request:
    branches: [ master ]
  workflow_dispatch:
  
  run-trufflehog:
    runs-on: ubuntu-latest
      - uses: actions/checkout@v2.3.2

      - uses: UnicornGlobal/trufflehog-actions-scan@master
        with:
         branch: ${{ github.ref }}
